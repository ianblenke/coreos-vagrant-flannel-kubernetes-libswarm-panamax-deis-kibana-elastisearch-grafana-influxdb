## converge.sh

This converge.sh script applies a set of cloud-init scripts that are applied on initial boot of a new CoreOS instance.

These are the actual cloud-init scripts I use in a production environment, with librato and newrelic credentials redacted.
